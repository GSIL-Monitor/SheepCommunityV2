namespace Sheep.Common.Settings
{
    /// <summary>
    ///     服务器主机配置的名称。
    /// </summary>
    public static class AppSettingsHostNames
    {
        // 是否开启调试模式。
        public const string DebugMode = "host.DebugMode";

        // 管理员身份秘钥。
        public const string AdminAuthSecret = "host.AdminAuthSecret";
    }
}